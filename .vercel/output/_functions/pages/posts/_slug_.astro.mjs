import { c as createAstro, a as createComponent, m as maybeRenderHead, d as addAttribute, r as renderComponent, b as renderTemplate, F as Fragment, u as unescapeHTML, e as renderTransition } from '../../chunks/astro/server_Bk0Qe8ay.mjs';
import 'kleur/colors';
import { g as getCollection } from '../../chunks/_astro_content_DWtfKiCL.mjs';
import { $ as $$LinkButton, s as socialIcons, a as $$Layout, b as $$Header, c as $$Footer } from '../../chunks/Footer_BKvCJzbM.mjs';
import { $ as $$Tag } from '../../chunks/Tag_BJdRuph6.mjs';
import { D as Datetime } from '../../chunks/Datetime_BA58FXGf.mjs';
import { s as slugifyStr } from '../../chunks/slugify_CvQuO4Tx.mjs';
/* empty css                                    */
import { S as SITE } from '../../chunks/config_BR15hYS5.mjs';
import 'clsx';
/* empty css                                    */
import { g as getSortedPosts } from '../../chunks/getSortedPosts_DkJw1amD.mjs';
export { renderers } from '../../renderers.mjs';

const $$Astro$3 = createAstro("https://vernonwu.com/");
const $$ShareLinks = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$3, $$props, $$slots);
  Astro2.self = $$ShareLinks;
  const URL = Astro2.url;
  const shareLinks = [
    {
      name: "WhatsApp",
      href: "https://wa.me/?text=",
      linkTitle: `Share this post via WhatsApp`
    },
    {
      name: "Facebook",
      href: "https://www.facebook.com/sharer.php?u=",
      linkTitle: `Share this post on Facebook`
    },
    {
      name: "Twitter",
      href: "https://twitter.com/intent/tweet?url=",
      linkTitle: `Tweet this post`
    },
    {
      name: "Telegram",
      href: "https://t.me/share/url?url=",
      linkTitle: `Share this post via Telegram`
    },
    {
      name: "Pinterest",
      href: "https://pinterest.com/pin/create/button/?url=",
      linkTitle: `Share this post on Pinterest`
    },
    {
      name: "Mail",
      href: "mailto:?subject=See%20this%20post&body=",
      linkTitle: `Share this post via email`
    }
  ];
  return renderTemplate`${maybeRenderHead()}<div${addAttribute(`social-icons astro-wkojbtzc`, "class")}> <span class="italic astro-wkojbtzc">Share this post on:</span> <div class="text-center astro-wkojbtzc"> ${shareLinks.map((social) => renderTemplate`${renderComponent($$result, "LinkButton", $$LinkButton, { "href": `${social.href + URL}`, "className": "link-button astro-wkojbtzc", "title": social.linkTitle }, { "default": ($$result2) => renderTemplate` ${renderComponent($$result2, "Fragment", Fragment, {}, { "default": ($$result3) => renderTemplate`${unescapeHTML(socialIcons[social.name])}` })} <span class="sr-only astro-wkojbtzc">${social.linkTitle}</span> ` })}`)} </div> </div> `;
}, "/Users/stevejobs/study/projects/vernonwu.com/src/components/ShareLinks.astro", void 0);

const $$Astro$2 = createAstro("https://vernonwu.com/");
const $$TableOfContents = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$2, $$props, $$slots);
  Astro2.self = $$TableOfContents;
  const { headings = [] } = Astro2.props;
  const tocTree = [];
  const parents = [];
  for (const heading of headings.filter(({ depth }) => depth >= 2 && depth <= 4)) {
    const node = { ...heading, children: [] };
    if (node.depth === 2) {
      tocTree.push(node);
      parents[0] = node;
      parents.length = 1;
      continue;
    }
    if (node.depth === 3 && parents[0]) {
      parents[0].children.push(node);
      parents[1] = node;
      parents.length = 2;
      continue;
    }
    if (node.depth === 4 && parents[1]) {
      parents[1].children.push(node);
      parents[2] = node;
      parents.length = 3;
    }
  }
  const hasHeadings = tocTree.length > 0;
  return renderTemplate`${hasHeadings && renderTemplate`${maybeRenderHead()}<nav aria-label="Table of contents" class="toc-card astro-xvrfupwn"><div class="toc-heading astro-xvrfupwn">On this page</div><ul class="space-y-1 astro-xvrfupwn">${tocTree.map((section, sectionIndex) => renderTemplate`<li class="group astro-xvrfupwn"${addAttribute(section.slug, "data-toc-item")}><a class="toc-link astro-xvrfupwn"${addAttribute(`#${section.slug}`, "href")}${addAttribute(section.slug, "data-toc-slug")}><span class="toc-badge astro-xvrfupwn">${sectionIndex + 1}</span><span class="toc-text astro-xvrfupwn">${section.text}</span></a>${section.children.length > 0 && renderTemplate`<ul class="toc-children astro-xvrfupwn">${section.children.map((subsection, subsectionIndex) => renderTemplate`<li class="group astro-xvrfupwn"${addAttribute(subsection.slug, "data-toc-item")}><a class="toc-link toc-sub astro-xvrfupwn"${addAttribute(`#${subsection.slug}`, "href")}${addAttribute(subsection.slug, "data-toc-slug")}><span class="toc-text toc-text-sub astro-xvrfupwn">${`${sectionIndex + 1}.${subsectionIndex + 1} ${subsection.text}`}</span></a>${subsection.children.length > 0 && renderTemplate`<ul class="toc-children astro-xvrfupwn">${subsection.children.map((topic, topicIndex) => renderTemplate`<li${addAttribute(topic.slug, "data-toc-item")} class="astro-xvrfupwn"><a class="toc-link toc-leaf astro-xvrfupwn"${addAttribute(`#${topic.slug}`, "href")}${addAttribute(topic.slug, "data-toc-slug")}><span class="toc-text toc-text-leaf astro-xvrfupwn">${`${sectionIndex + 1}.${subsectionIndex + 1}.${topicIndex + 1} ${topic.text}`}</span></a></li>`)}</ul>`}</li>`)}</ul>`}</li>`)}</ul></nav>`}`;
}, "/Users/stevejobs/study/projects/vernonwu.com/src/components/TableOfContents.astro", void 0);

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Astro$1 = createAstro("https://vernonwu.com/");
const $$PostDetails = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1, $$props, $$slots);
  Astro2.self = $$PostDetails;
  const { post, posts } = Astro2.props;
  const {
    title,
    author,
    description,
    ogImage,
    canonicalURL,
    pubDatetime,
    modDatetime,
    tags,
    editPost
  } = post.data;
  const { Content, headings } = await post.render();
  const ogImageUrl = typeof ogImage === "string" ? ogImage : ogImage?.src;
  const ogUrl = new URL(
    ogImageUrl ?? `/posts/${slugifyStr(title)}.png`,
    Astro2.url.origin
  ).href;
  const layoutProps = {
    title: `${title} | ${SITE.title}`,
    author,
    description,
    pubDatetime,
    modDatetime,
    canonicalURL,
    ogImage: ogUrl,
    scrollSmooth: true
  };
  const allPosts = posts.map(({ data: { title: title2 }, slug }) => ({
    slug,
    title: title2
  }));
  const currentPostIndex = allPosts.findIndex((a) => a.slug === post.slug);
  const prevPost = currentPostIndex !== 0 ? allPosts[currentPostIndex - 1] : null;
  const nextPost = currentPostIndex !== allPosts.length ? allPosts[currentPostIndex + 1] : null;
  return renderTemplate(_a || (_a = __template(["", '  <script data-astro-rerun>\n  /** Create a progress indicator\n   *  at the top */\n  function createProgressBar() {\n    // Create the main container div\n    const progressContainer = document.createElement("div");\n    progressContainer.className =\n      "progress-container fixed top-0 z-10 h-1 w-full bg-skin-fill";\n\n    // Create the progress bar div\n    const progressBar = document.createElement("div");\n    progressBar.className = "progress-bar h-1 w-0 bg-skin-accent";\n    progressBar.id = "myBar";\n\n    // Append the progress bar to the progress container\n    progressContainer.appendChild(progressBar);\n\n    // Append the progress container to the document body or any other desired parent element\n    document.body.appendChild(progressContainer);\n  }\n  createProgressBar();\n\n  // Position ToC relative to its spacer so it stays centered and aligned\n  function positionToc() {\n    const toc = document.querySelector(".toc-wrapper");\n    const spacer = document.querySelector(".toc-spacer");\n    if (!toc || !spacer) return;\n\n    const rect = spacer.getBoundingClientRect();\n    toc.style.left = `${rect.left}px`;\n  }\n  const schedulePositionToc = () => requestAnimationFrame(positionToc);\n  schedulePositionToc();\n  window.addEventListener("resize", schedulePositionToc, { passive: true });\n\n  // Highlight current section and subsection in the ToC\n  let cleanupTocHighlight;\n  let cleanupRevealOnScroll;\n  let cleanupImageViewer;\n  function initTocHighlight() {\n    const articleHeadings = Array.from(\n      document.querySelectorAll("#article h2, #article h3, #article h4")\n    );\n    const tocLinks = new Map(\n      Array.from(\n        document.querySelectorAll(".toc-link[data-toc-slug]"),\n        link => [link.getAttribute("data-toc-slug"), link]\n      )\n    );\n    const tocItems = new Map(\n      Array.from(\n        document.querySelectorAll("[data-toc-item]"),\n        item => [item.getAttribute("data-toc-item"), item]\n      )\n    );\n\n    if (!articleHeadings.length || tocLinks.size === 0 || tocItems.size === 0) return;\n\n    const parentOf = new Map();\n    let lastH2 = null;\n    let lastH3 = null;\n    for (const heading of articleHeadings) {\n      if (heading.tagName === "H2") {\n        lastH2 = heading.id;\n        lastH3 = null;\n        parentOf.set(heading.id, null);\n      } else if (heading.tagName === "H3") {\n        parentOf.set(heading.id, lastH2);\n        lastH3 = heading.id;\n      } else if (heading.tagName === "H4") {\n        parentOf.set(heading.id, lastH3 ?? lastH2);\n      }\n    }\n\n    const clearActive = () =>\n      tocLinks.forEach(link => link.classList.remove("active"));\n\n    const clearOpen = () => tocItems.forEach(item => item.classList.remove("open"));\n\n    const markActiveChain = slug => {\n      const visited = new Set();\n      let current = slug;\n      while (current && !visited.has(current)) {\n        visited.add(current);\n        tocLinks.get(current)?.classList.add("active");\n        current = parentOf.get(current) ?? null;\n      }\n    };\n\n    const markOpenChain = slug => {\n      const visited = new Set();\n      let current = slug;\n      while (current && !visited.has(current)) {\n        visited.add(current);\n        tocItems.get(current)?.classList.add("open");\n        current = parentOf.get(current) ?? null;\n      }\n    };\n\n    const getCurrentSlug = () => {\n      const threshold = window.scrollY + window.innerHeight * 0.25;\n      let current = articleHeadings[0]?.id ?? null;\n      for (const heading of articleHeadings) {\n        if (heading.offsetTop <= threshold) {\n          current = heading.id;\n        } else {\n          break;\n        }\n      }\n      return current;\n    };\n\n    let activeSlug = null;\n    const updateActive = () => {\n      const slug = getCurrentSlug();\n      if (!slug || slug === activeSlug) return;\n      activeSlug = slug;\n      clearActive();\n      clearOpen();\n      markActiveChain(slug);\n      markOpenChain(slug);\n    };\n\n    const onScroll = () => requestAnimationFrame(updateActive);\n    const onResize = () => requestAnimationFrame(updateActive);\n\n    document.addEventListener("scroll", onScroll, { passive: true });\n    window.addEventListener("resize", onResize, { passive: true });\n    updateActive();\n\n    return () => {\n      document.removeEventListener("scroll", onScroll);\n      window.removeEventListener("resize", onResize);\n    };\n  }\n  cleanupTocHighlight = initTocHighlight();\n\n  // Reveal selected blocks on scroll for mobile screens\n  function initRevealOnScroll() {\n    const targets = Array.from(document.querySelectorAll("[data-reveal-target]"));\n    if (!targets.length) return;\n\n    const mediaQuery = window.matchMedia("(max-width: 640px)");\n    let observer = null;\n\n    const disconnect = () => observer?.disconnect();\n\n    const showAll = () => {\n      targets.forEach(el => el.classList.add("revealed"));\n    };\n\n    const setupObserver = () => {\n      disconnect();\n      if (!mediaQuery.matches) {\n        showAll();\n        return;\n      }\n\n      observer = new IntersectionObserver(\n        entries => {\n          entries.forEach(entry => {\n            if (entry.isIntersecting) {\n              entry.target.classList.add("revealed");\n              observer?.unobserve(entry.target);\n            }\n          });\n        },\n        { threshold: 0.15 }\n      );\n\n      targets.forEach(el => {\n        // Remove stale visibility when switching breakpoints\n        el.classList.remove("revealed");\n        observer?.observe(el);\n      });\n    };\n\n    setupObserver();\n    mediaQuery.addEventListener("change", setupObserver);\n\n    return () => {\n      disconnect();\n      mediaQuery.removeEventListener("change", setupObserver);\n    };\n  }\n  cleanupRevealOnScroll = initRevealOnScroll();\n\n  // Full-screen image viewer for blog images\n  function initImageViewer() {\n    const viewer = document.getElementById("image-viewer");\n    const viewerImg = document.getElementById("image-viewer-img");\n    if (!viewer || !viewerImg) return;\n    viewerImg.setAttribute("draggable", "false");\n\n    const articleImages = Array.from(document.querySelectorAll("#article img"));\n    if (!articleImages.length) return;\n\n    const listeners = new Map();\n    let activeImage = null;\n    let scale = 1;\n    let translateX = 0;\n    let translateY = 0;\n    let isDragging = false;\n    let dragStartX = 0;\n    let dragStartY = 0;\n    let dragOriginX = 0;\n    let dragOriginY = 0;\n\n    const applyScale = () => {\n      viewerImg.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;\n      const pannable = scale !== 1 || translateX !== 0 || translateY !== 0;\n      viewerImg.style.cursor = pannable ? (isDragging ? "grabbing" : "grab") : "auto";\n    };\n\n    const open = img => {\n      const src = img.getAttribute("src");\n      if (!src) return;\n      activeImage = img;\n      viewerImg.src = src;\n      viewerImg.alt = img.getAttribute("alt") ?? "";\n      scale = 1;\n      translateX = 0;\n      translateY = 0;\n      applyScale();\n      viewer.classList.add("open");\n      viewer.removeAttribute("hidden");\n      document.body.classList.add("image-viewer-open");\n      viewer.tabIndex = -1;\n      viewer.focus({ preventScroll: true });\n    };\n\n    const close = () => {\n      viewer.classList.remove("open");\n      viewer.setAttribute("hidden", "true");\n      viewerImg.src = "";\n      scale = 1;\n      translateX = 0;\n      translateY = 0;\n      applyScale();\n      document.body.classList.remove("image-viewer-open");\n\n      if (activeImage?.isConnected) {\n        activeImage.focus({ preventScroll: true });\n      }\n      activeImage = null;\n    };\n\n    const onBackdropClick = event => {\n      if (event.target === viewer) {\n        close();\n      }\n    };\n\n    const onEsc = event => {\n      if (event.key === "Escape" && viewer.classList.contains("open")) {\n        close();\n      }\n    };\n\n    const onWheel = event => {\n      if (!viewer.classList.contains("open")) return;\n      event.preventDefault();\n      const delta = event.deltaY;\n      const direction = delta > 0 ? -1 : 1;\n      const next = Math.min(4, Math.max(0.75, scale + direction * 0.2));\n      if (next === scale) return;\n      scale = next;\n      applyScale();\n    };\n\n    const onDragStart = event => {\n      event.preventDefault();\n    };\n\n    const onPointerDown = event => {\n      if (!viewer.classList.contains("open")) return;\n      if (event.button !== 0) return;\n      isDragging = true;\n      dragStartX = event.clientX;\n      dragStartY = event.clientY;\n      dragOriginX = translateX;\n      dragOriginY = translateY;\n      viewerImg.setPointerCapture(event.pointerId);\n      applyScale();\n    };\n\n    const onPointerMove = event => {\n      if (!isDragging) return;\n      event.preventDefault();\n      const deltaX = event.clientX - dragStartX;\n      const deltaY = event.clientY - dragStartY;\n      translateX = dragOriginX + deltaX;\n      translateY = dragOriginY + deltaY;\n      applyScale();\n    };\n\n    const endDrag = event => {\n      if (!isDragging) return;\n      isDragging = false;\n      if (event?.pointerId != null) {\n        viewerImg.releasePointerCapture(event.pointerId);\n      }\n      applyScale();\n    };\n\n    articleImages.forEach(img => {\n      const onClick = event => {\n        event?.preventDefault();\n        event?.stopPropagation();\n        open(img);\n      };\n      const onKeyDown = event => {\n        if (event.key === "Enter" || event.key === " ") {\n          event.preventDefault();\n          event.stopPropagation();\n          open(img);\n        }\n      };\n\n      const hadTabIndex = img.hasAttribute("tabindex");\n      if (!hadTabIndex) {\n        img.tabIndex = 0;\n      }\n\n      img.classList.add("zoomable-image");\n      img.addEventListener("click", onClick);\n      img.addEventListener("keydown", onKeyDown);\n      listeners.set(img, { onClick, onKeyDown, hadTabIndex });\n    });\n\n    viewer.addEventListener("click", onBackdropClick);\n    viewer.addEventListener("wheel", onWheel, { passive: false });\n    viewerImg.addEventListener("dragstart", onDragStart);\n    viewerImg.addEventListener("pointerdown", onPointerDown);\n    viewerImg.addEventListener("pointermove", onPointerMove);\n    viewerImg.addEventListener("pointerup", endDrag);\n    viewerImg.addEventListener("pointercancel", endDrag);\n    viewerImg.addEventListener("pointerleave", endDrag);\n    document.addEventListener("keydown", onEsc);\n\n    return () => {\n      close();\n      articleImages.forEach(img => {\n        const { onClick, onKeyDown, hadTabIndex } = listeners.get(img) ?? {};\n        if (onClick) img.removeEventListener("click", onClick);\n        if (onKeyDown) img.removeEventListener("keydown", onKeyDown);\n        if (!hadTabIndex && img.tabIndex === 0) {\n          img.removeAttribute("tabindex");\n        }\n        img.classList.remove("zoomable-image");\n      });\n\n      viewer.removeEventListener("click", onBackdropClick);\n      viewer.removeEventListener("wheel", onWheel);\n      viewerImg.removeEventListener("dragstart", onDragStart);\n      viewerImg.removeEventListener("pointerdown", onPointerDown);\n      viewerImg.removeEventListener("pointermove", onPointerMove);\n      viewerImg.removeEventListener("pointerup", endDrag);\n      viewerImg.removeEventListener("pointercancel", endDrag);\n      viewerImg.removeEventListener("pointerleave", endDrag);\n      document.removeEventListener("keydown", onEsc);\n      document.body.classList.remove("image-viewer-open");\n    };\n  }\n  cleanupImageViewer = initImageViewer();\n\n  /** Update the progress bar\n   *  when user scrolls */\n  function updateScrollProgress() {\n    document.addEventListener("scroll", () => {\n      const winScroll =\n        document.body.scrollTop || document.documentElement.scrollTop;\n      const height =\n        document.documentElement.scrollHeight -\n        document.documentElement.clientHeight;\n      const scrolled = (winScroll / height) * 100;\n      if (document) {\n        const myBar = document.getElementById("myBar");\n        if (myBar) {\n          myBar.style.width = scrolled + "%";\n        }\n      }\n    });\n  }\n  updateScrollProgress();\n\n  /** Attaches links to headings in the document,\n   *  allowing sharing of sections easily */\n  function addHeadingLinks() {\n    const headings = Array.from(\n      document.querySelectorAll("h2, h3, h4, h5, h6")\n    );\n    for (const heading of headings) {\n      heading.classList.add("group");\n      const link = document.createElement("a");\n      link.className =\n        "heading-link ml-2 opacity-0 group-hover:opacity-100 focus:opacity-100";\n      link.href = "#" + heading.id;\n\n      const span = document.createElement("span");\n      span.ariaHidden = "true";\n      span.innerText = "#";\n      link.appendChild(span);\n      heading.appendChild(link);\n    }\n  }\n  addHeadingLinks();\n\n  /** Attaches copy buttons to code blocks in the document,\n   * allowing users to copy code easily. */\n  function attachCopyButtons() {\n    const copyIcon = "\u29C9";\n    const copiedIcon = "\u2713";\n    const codeBlocks = Array.from(document.querySelectorAll("pre"));\n\n    const getLanguage = block => {\n      const dataLang = block.getAttribute("data-language");\n      if (dataLang) return dataLang;\n      const codeEl = block.querySelector("code");\n      const classNames = [\n        ...block.classList,\n        ...(codeEl?.classList ?? []),\n      ];\n      for (const cls of classNames) {\n        const match = cls.match(/(?:lang|language)-(.+)/i);\n        if (match?.[1]) return match[1];\n      }\n      return "text";\n    };\n\n    const formatLanguageLabel = lang => {\n      const key = (lang ?? "text").toLowerCase();\n      const nameMap = {\n        js: "javascript",\n        ts: "typescript",\n        py: "python",\n        rb: "ruby",\n        rs: "rust",\n        sh: "shell",\n        zsh: "shell",\n        bash: "shell",\n        yml: "yaml",\n        md: "markdown",\n        cplusplus: "cpp",\n        cs: "csharp",\n      };\n      const normalized = nameMap[key] ?? key;\n      const cleaned = normalized.replace(/[\\s_-]+/g, " ").trim();\n      return (cleaned || "text").toUpperCase();\n    };\n\n    for (const codeBlock of codeBlocks) {\n      // avoid double-wrapping if already enhanced\n      if (codeBlock.parentElement?.classList.contains("code-block-wrapper")) {\n        continue;\n      }\n\n      const wrapper = document.createElement("div");\n      wrapper.className = "code-block-wrapper";\n\n      const copyButton = document.createElement("button");\n      copyButton.type = "button";\n      copyButton.className = "copy-code";\n      copyButton.setAttribute("aria-label", "Copy code");\n      copyButton.innerHTML = copyIcon;\n      codeBlock.setAttribute("tabindex", "0");\n\n      const languageLabel = document.createElement("div");\n      languageLabel.className = "code-language";\n      languageLabel.textContent = formatLanguageLabel(getLanguage(codeBlock));\n\n      // wrap code block with relative parent element and place overlays on top\n      codeBlock?.parentNode?.insertBefore(wrapper, codeBlock);\n      wrapper.appendChild(codeBlock);\n      wrapper.appendChild(copyButton);\n      wrapper.appendChild(languageLabel);\n\n      copyButton.addEventListener("click", async () => {\n        await copyCode(codeBlock, copyButton);\n      });\n\n      // when pointer leaves, drop button focus so the language label returns\n      wrapper.addEventListener("pointerleave", () => {\n        if (document.activeElement === copyButton) {\n          copyButton.blur();\n        }\n      });\n    }\n\n    async function copyCode(block, button) {\n      const code = block.querySelector("code");\n      const text = code?.innerText;\n\n      await navigator.clipboard.writeText(text ?? "");\n\n      // visual feedback that task is completed\n      button.innerText = copiedIcon;\n\n      setTimeout(() => {\n        button.innerText = copyIcon;\n      }, 1400);\n    }\n  }\n  attachCopyButtons();\n\n  /** Scrolls the document to the top when\n   * the "Back to Top" button is clicked. */\n  function backToTop() {\n    document.querySelector("#back-to-top")?.addEventListener("click", () => {\n      document.body.scrollTop = 0; // For Safari\n      document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera\n    });\n  }\n  backToTop();\n\n  /* Go to page start after page swap */\n  document.addEventListener("astro:after-swap", () => {\n    cleanupTocHighlight?.();\n    cleanupRevealOnScroll?.();\n    cleanupImageViewer?.();\n    cleanupTocHighlight = initTocHighlight();\n    cleanupRevealOnScroll = initRevealOnScroll();\n    cleanupImageViewer = initImageViewer();\n    attachCopyButtons();\n    schedulePositionToc();\n    window.scrollTo({ left: 0, top: 0, behavior: "instant" });\n  });\n<\/script>'], ["", '  <script data-astro-rerun>\n  /** Create a progress indicator\n   *  at the top */\n  function createProgressBar() {\n    // Create the main container div\n    const progressContainer = document.createElement("div");\n    progressContainer.className =\n      "progress-container fixed top-0 z-10 h-1 w-full bg-skin-fill";\n\n    // Create the progress bar div\n    const progressBar = document.createElement("div");\n    progressBar.className = "progress-bar h-1 w-0 bg-skin-accent";\n    progressBar.id = "myBar";\n\n    // Append the progress bar to the progress container\n    progressContainer.appendChild(progressBar);\n\n    // Append the progress container to the document body or any other desired parent element\n    document.body.appendChild(progressContainer);\n  }\n  createProgressBar();\n\n  // Position ToC relative to its spacer so it stays centered and aligned\n  function positionToc() {\n    const toc = document.querySelector(".toc-wrapper");\n    const spacer = document.querySelector(".toc-spacer");\n    if (!toc || !spacer) return;\n\n    const rect = spacer.getBoundingClientRect();\n    toc.style.left = \\`\\${rect.left}px\\`;\n  }\n  const schedulePositionToc = () => requestAnimationFrame(positionToc);\n  schedulePositionToc();\n  window.addEventListener("resize", schedulePositionToc, { passive: true });\n\n  // Highlight current section and subsection in the ToC\n  let cleanupTocHighlight;\n  let cleanupRevealOnScroll;\n  let cleanupImageViewer;\n  function initTocHighlight() {\n    const articleHeadings = Array.from(\n      document.querySelectorAll("#article h2, #article h3, #article h4")\n    );\n    const tocLinks = new Map(\n      Array.from(\n        document.querySelectorAll(".toc-link[data-toc-slug]"),\n        link => [link.getAttribute("data-toc-slug"), link]\n      )\n    );\n    const tocItems = new Map(\n      Array.from(\n        document.querySelectorAll("[data-toc-item]"),\n        item => [item.getAttribute("data-toc-item"), item]\n      )\n    );\n\n    if (!articleHeadings.length || tocLinks.size === 0 || tocItems.size === 0) return;\n\n    const parentOf = new Map();\n    let lastH2 = null;\n    let lastH3 = null;\n    for (const heading of articleHeadings) {\n      if (heading.tagName === "H2") {\n        lastH2 = heading.id;\n        lastH3 = null;\n        parentOf.set(heading.id, null);\n      } else if (heading.tagName === "H3") {\n        parentOf.set(heading.id, lastH2);\n        lastH3 = heading.id;\n      } else if (heading.tagName === "H4") {\n        parentOf.set(heading.id, lastH3 ?? lastH2);\n      }\n    }\n\n    const clearActive = () =>\n      tocLinks.forEach(link => link.classList.remove("active"));\n\n    const clearOpen = () => tocItems.forEach(item => item.classList.remove("open"));\n\n    const markActiveChain = slug => {\n      const visited = new Set();\n      let current = slug;\n      while (current && !visited.has(current)) {\n        visited.add(current);\n        tocLinks.get(current)?.classList.add("active");\n        current = parentOf.get(current) ?? null;\n      }\n    };\n\n    const markOpenChain = slug => {\n      const visited = new Set();\n      let current = slug;\n      while (current && !visited.has(current)) {\n        visited.add(current);\n        tocItems.get(current)?.classList.add("open");\n        current = parentOf.get(current) ?? null;\n      }\n    };\n\n    const getCurrentSlug = () => {\n      const threshold = window.scrollY + window.innerHeight * 0.25;\n      let current = articleHeadings[0]?.id ?? null;\n      for (const heading of articleHeadings) {\n        if (heading.offsetTop <= threshold) {\n          current = heading.id;\n        } else {\n          break;\n        }\n      }\n      return current;\n    };\n\n    let activeSlug = null;\n    const updateActive = () => {\n      const slug = getCurrentSlug();\n      if (!slug || slug === activeSlug) return;\n      activeSlug = slug;\n      clearActive();\n      clearOpen();\n      markActiveChain(slug);\n      markOpenChain(slug);\n    };\n\n    const onScroll = () => requestAnimationFrame(updateActive);\n    const onResize = () => requestAnimationFrame(updateActive);\n\n    document.addEventListener("scroll", onScroll, { passive: true });\n    window.addEventListener("resize", onResize, { passive: true });\n    updateActive();\n\n    return () => {\n      document.removeEventListener("scroll", onScroll);\n      window.removeEventListener("resize", onResize);\n    };\n  }\n  cleanupTocHighlight = initTocHighlight();\n\n  // Reveal selected blocks on scroll for mobile screens\n  function initRevealOnScroll() {\n    const targets = Array.from(document.querySelectorAll("[data-reveal-target]"));\n    if (!targets.length) return;\n\n    const mediaQuery = window.matchMedia("(max-width: 640px)");\n    let observer = null;\n\n    const disconnect = () => observer?.disconnect();\n\n    const showAll = () => {\n      targets.forEach(el => el.classList.add("revealed"));\n    };\n\n    const setupObserver = () => {\n      disconnect();\n      if (!mediaQuery.matches) {\n        showAll();\n        return;\n      }\n\n      observer = new IntersectionObserver(\n        entries => {\n          entries.forEach(entry => {\n            if (entry.isIntersecting) {\n              entry.target.classList.add("revealed");\n              observer?.unobserve(entry.target);\n            }\n          });\n        },\n        { threshold: 0.15 }\n      );\n\n      targets.forEach(el => {\n        // Remove stale visibility when switching breakpoints\n        el.classList.remove("revealed");\n        observer?.observe(el);\n      });\n    };\n\n    setupObserver();\n    mediaQuery.addEventListener("change", setupObserver);\n\n    return () => {\n      disconnect();\n      mediaQuery.removeEventListener("change", setupObserver);\n    };\n  }\n  cleanupRevealOnScroll = initRevealOnScroll();\n\n  // Full-screen image viewer for blog images\n  function initImageViewer() {\n    const viewer = document.getElementById("image-viewer");\n    const viewerImg = document.getElementById("image-viewer-img");\n    if (!viewer || !viewerImg) return;\n    viewerImg.setAttribute("draggable", "false");\n\n    const articleImages = Array.from(document.querySelectorAll("#article img"));\n    if (!articleImages.length) return;\n\n    const listeners = new Map();\n    let activeImage = null;\n    let scale = 1;\n    let translateX = 0;\n    let translateY = 0;\n    let isDragging = false;\n    let dragStartX = 0;\n    let dragStartY = 0;\n    let dragOriginX = 0;\n    let dragOriginY = 0;\n\n    const applyScale = () => {\n      viewerImg.style.transform = \\`translate(\\${translateX}px, \\${translateY}px) scale(\\${scale})\\`;\n      const pannable = scale !== 1 || translateX !== 0 || translateY !== 0;\n      viewerImg.style.cursor = pannable ? (isDragging ? "grabbing" : "grab") : "auto";\n    };\n\n    const open = img => {\n      const src = img.getAttribute("src");\n      if (!src) return;\n      activeImage = img;\n      viewerImg.src = src;\n      viewerImg.alt = img.getAttribute("alt") ?? "";\n      scale = 1;\n      translateX = 0;\n      translateY = 0;\n      applyScale();\n      viewer.classList.add("open");\n      viewer.removeAttribute("hidden");\n      document.body.classList.add("image-viewer-open");\n      viewer.tabIndex = -1;\n      viewer.focus({ preventScroll: true });\n    };\n\n    const close = () => {\n      viewer.classList.remove("open");\n      viewer.setAttribute("hidden", "true");\n      viewerImg.src = "";\n      scale = 1;\n      translateX = 0;\n      translateY = 0;\n      applyScale();\n      document.body.classList.remove("image-viewer-open");\n\n      if (activeImage?.isConnected) {\n        activeImage.focus({ preventScroll: true });\n      }\n      activeImage = null;\n    };\n\n    const onBackdropClick = event => {\n      if (event.target === viewer) {\n        close();\n      }\n    };\n\n    const onEsc = event => {\n      if (event.key === "Escape" && viewer.classList.contains("open")) {\n        close();\n      }\n    };\n\n    const onWheel = event => {\n      if (!viewer.classList.contains("open")) return;\n      event.preventDefault();\n      const delta = event.deltaY;\n      const direction = delta > 0 ? -1 : 1;\n      const next = Math.min(4, Math.max(0.75, scale + direction * 0.2));\n      if (next === scale) return;\n      scale = next;\n      applyScale();\n    };\n\n    const onDragStart = event => {\n      event.preventDefault();\n    };\n\n    const onPointerDown = event => {\n      if (!viewer.classList.contains("open")) return;\n      if (event.button !== 0) return;\n      isDragging = true;\n      dragStartX = event.clientX;\n      dragStartY = event.clientY;\n      dragOriginX = translateX;\n      dragOriginY = translateY;\n      viewerImg.setPointerCapture(event.pointerId);\n      applyScale();\n    };\n\n    const onPointerMove = event => {\n      if (!isDragging) return;\n      event.preventDefault();\n      const deltaX = event.clientX - dragStartX;\n      const deltaY = event.clientY - dragStartY;\n      translateX = dragOriginX + deltaX;\n      translateY = dragOriginY + deltaY;\n      applyScale();\n    };\n\n    const endDrag = event => {\n      if (!isDragging) return;\n      isDragging = false;\n      if (event?.pointerId != null) {\n        viewerImg.releasePointerCapture(event.pointerId);\n      }\n      applyScale();\n    };\n\n    articleImages.forEach(img => {\n      const onClick = event => {\n        event?.preventDefault();\n        event?.stopPropagation();\n        open(img);\n      };\n      const onKeyDown = event => {\n        if (event.key === "Enter" || event.key === " ") {\n          event.preventDefault();\n          event.stopPropagation();\n          open(img);\n        }\n      };\n\n      const hadTabIndex = img.hasAttribute("tabindex");\n      if (!hadTabIndex) {\n        img.tabIndex = 0;\n      }\n\n      img.classList.add("zoomable-image");\n      img.addEventListener("click", onClick);\n      img.addEventListener("keydown", onKeyDown);\n      listeners.set(img, { onClick, onKeyDown, hadTabIndex });\n    });\n\n    viewer.addEventListener("click", onBackdropClick);\n    viewer.addEventListener("wheel", onWheel, { passive: false });\n    viewerImg.addEventListener("dragstart", onDragStart);\n    viewerImg.addEventListener("pointerdown", onPointerDown);\n    viewerImg.addEventListener("pointermove", onPointerMove);\n    viewerImg.addEventListener("pointerup", endDrag);\n    viewerImg.addEventListener("pointercancel", endDrag);\n    viewerImg.addEventListener("pointerleave", endDrag);\n    document.addEventListener("keydown", onEsc);\n\n    return () => {\n      close();\n      articleImages.forEach(img => {\n        const { onClick, onKeyDown, hadTabIndex } = listeners.get(img) ?? {};\n        if (onClick) img.removeEventListener("click", onClick);\n        if (onKeyDown) img.removeEventListener("keydown", onKeyDown);\n        if (!hadTabIndex && img.tabIndex === 0) {\n          img.removeAttribute("tabindex");\n        }\n        img.classList.remove("zoomable-image");\n      });\n\n      viewer.removeEventListener("click", onBackdropClick);\n      viewer.removeEventListener("wheel", onWheel);\n      viewerImg.removeEventListener("dragstart", onDragStart);\n      viewerImg.removeEventListener("pointerdown", onPointerDown);\n      viewerImg.removeEventListener("pointermove", onPointerMove);\n      viewerImg.removeEventListener("pointerup", endDrag);\n      viewerImg.removeEventListener("pointercancel", endDrag);\n      viewerImg.removeEventListener("pointerleave", endDrag);\n      document.removeEventListener("keydown", onEsc);\n      document.body.classList.remove("image-viewer-open");\n    };\n  }\n  cleanupImageViewer = initImageViewer();\n\n  /** Update the progress bar\n   *  when user scrolls */\n  function updateScrollProgress() {\n    document.addEventListener("scroll", () => {\n      const winScroll =\n        document.body.scrollTop || document.documentElement.scrollTop;\n      const height =\n        document.documentElement.scrollHeight -\n        document.documentElement.clientHeight;\n      const scrolled = (winScroll / height) * 100;\n      if (document) {\n        const myBar = document.getElementById("myBar");\n        if (myBar) {\n          myBar.style.width = scrolled + "%";\n        }\n      }\n    });\n  }\n  updateScrollProgress();\n\n  /** Attaches links to headings in the document,\n   *  allowing sharing of sections easily */\n  function addHeadingLinks() {\n    const headings = Array.from(\n      document.querySelectorAll("h2, h3, h4, h5, h6")\n    );\n    for (const heading of headings) {\n      heading.classList.add("group");\n      const link = document.createElement("a");\n      link.className =\n        "heading-link ml-2 opacity-0 group-hover:opacity-100 focus:opacity-100";\n      link.href = "#" + heading.id;\n\n      const span = document.createElement("span");\n      span.ariaHidden = "true";\n      span.innerText = "#";\n      link.appendChild(span);\n      heading.appendChild(link);\n    }\n  }\n  addHeadingLinks();\n\n  /** Attaches copy buttons to code blocks in the document,\n   * allowing users to copy code easily. */\n  function attachCopyButtons() {\n    const copyIcon = "\u29C9";\n    const copiedIcon = "\u2713";\n    const codeBlocks = Array.from(document.querySelectorAll("pre"));\n\n    const getLanguage = block => {\n      const dataLang = block.getAttribute("data-language");\n      if (dataLang) return dataLang;\n      const codeEl = block.querySelector("code");\n      const classNames = [\n        ...block.classList,\n        ...(codeEl?.classList ?? []),\n      ];\n      for (const cls of classNames) {\n        const match = cls.match(/(?:lang|language)-(.+)/i);\n        if (match?.[1]) return match[1];\n      }\n      return "text";\n    };\n\n    const formatLanguageLabel = lang => {\n      const key = (lang ?? "text").toLowerCase();\n      const nameMap = {\n        js: "javascript",\n        ts: "typescript",\n        py: "python",\n        rb: "ruby",\n        rs: "rust",\n        sh: "shell",\n        zsh: "shell",\n        bash: "shell",\n        yml: "yaml",\n        md: "markdown",\n        cplusplus: "cpp",\n        cs: "csharp",\n      };\n      const normalized = nameMap[key] ?? key;\n      const cleaned = normalized.replace(/[\\\\s_-]+/g, " ").trim();\n      return (cleaned || "text").toUpperCase();\n    };\n\n    for (const codeBlock of codeBlocks) {\n      // avoid double-wrapping if already enhanced\n      if (codeBlock.parentElement?.classList.contains("code-block-wrapper")) {\n        continue;\n      }\n\n      const wrapper = document.createElement("div");\n      wrapper.className = "code-block-wrapper";\n\n      const copyButton = document.createElement("button");\n      copyButton.type = "button";\n      copyButton.className = "copy-code";\n      copyButton.setAttribute("aria-label", "Copy code");\n      copyButton.innerHTML = copyIcon;\n      codeBlock.setAttribute("tabindex", "0");\n\n      const languageLabel = document.createElement("div");\n      languageLabel.className = "code-language";\n      languageLabel.textContent = formatLanguageLabel(getLanguage(codeBlock));\n\n      // wrap code block with relative parent element and place overlays on top\n      codeBlock?.parentNode?.insertBefore(wrapper, codeBlock);\n      wrapper.appendChild(codeBlock);\n      wrapper.appendChild(copyButton);\n      wrapper.appendChild(languageLabel);\n\n      copyButton.addEventListener("click", async () => {\n        await copyCode(codeBlock, copyButton);\n      });\n\n      // when pointer leaves, drop button focus so the language label returns\n      wrapper.addEventListener("pointerleave", () => {\n        if (document.activeElement === copyButton) {\n          copyButton.blur();\n        }\n      });\n    }\n\n    async function copyCode(block, button) {\n      const code = block.querySelector("code");\n      const text = code?.innerText;\n\n      await navigator.clipboard.writeText(text ?? "");\n\n      // visual feedback that task is completed\n      button.innerText = copiedIcon;\n\n      setTimeout(() => {\n        button.innerText = copyIcon;\n      }, 1400);\n    }\n  }\n  attachCopyButtons();\n\n  /** Scrolls the document to the top when\n   * the "Back to Top" button is clicked. */\n  function backToTop() {\n    document.querySelector("#back-to-top")?.addEventListener("click", () => {\n      document.body.scrollTop = 0; // For Safari\n      document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera\n    });\n  }\n  backToTop();\n\n  /* Go to page start after page swap */\n  document.addEventListener("astro:after-swap", () => {\n    cleanupTocHighlight?.();\n    cleanupRevealOnScroll?.();\n    cleanupImageViewer?.();\n    cleanupTocHighlight = initTocHighlight();\n    cleanupRevealOnScroll = initRevealOnScroll();\n    cleanupImageViewer = initImageViewer();\n    attachCopyButtons();\n    schedulePositionToc();\n    window.scrollTo({ left: 0, top: 0, behavior: "instant" });\n  });\n<\/script>'])), renderComponent($$result, "Layout", $$Layout, { ...layoutProps, "class": "astro-vj4tpspi" }, { "default": async ($$result2) => renderTemplate` ${renderComponent($$result2, "Header", $$Header, { "class": "astro-vj4tpspi" })} ${maybeRenderHead()}<main id="main-content" class="mx-auto flex w-full max-w-6xl flex-col gap-8 px-4 pb-12 sm:flex-row sm:items-start sm:gap-10 astro-vj4tpspi"> <div class="toc-spacer hidden w-[16rem] shrink-0 sm:block astro-vj4tpspi"> <aside class="toc-wrapper fixed top-[15%] w-[16rem] astro-vj4tpspi"> ${renderComponent($$result2, "TableOfContents", $$TableOfContents, { "headings": headings, "class": "astro-vj4tpspi" })} </aside> </div> <div class="min-w-0 flex-1 astro-vj4tpspi"> <div class="mx-auto w-full max-w-3xl astro-vj4tpspi"> <div class="mb-2 mt-8 flex justify-start astro-vj4tpspi"> <button class="focus-outline flex hover:opacity-75 astro-vj4tpspi" onclick="(() => (history.length === 1) ? window.location = '/' : history.back())()"> <svg xmlns="http://www.w3.org/2000/svg" class="astro-vj4tpspi"><path d="M13.293 6.293 7.586 12l5.707 5.707 1.414-1.414L10.414 12l4.293-4.293z" class="astro-vj4tpspi"></path> </svg><span class="astro-vj4tpspi">Go back</span> </button> </div> <h1 class="post-title reveal-on-scroll astro-vj4tpspi" data-reveal-target${addAttribute(renderTransition($$result2, "nda7toff", "", slugifyStr(title)), "data-astro-transition-scope")}> ${title} </h1> ${renderComponent($$result2, "Datetime", Datetime, { "pubDatetime": pubDatetime, "modDatetime": modDatetime, "size": "lg", "className": "my-2 astro-vj4tpspi", "editPost": editPost, "postId": post.id })} <article id="article" class="prose mx-auto mt-8 max-w-3xl astro-vj4tpspi"> ${renderComponent($$result2, "Content", Content, { "class": "astro-vj4tpspi" })} </article> <ul class="my-8 astro-vj4tpspi"> ${tags.map((tag) => renderTemplate`${renderComponent($$result2, "Tag", $$Tag, { "tag": slugifyStr(tag), "class": "astro-vj4tpspi" })}`)} </ul> <div class="flex flex-col-reverse items-center justify-between gap-6 sm:flex-row-reverse sm:items-end sm:gap-4 astro-vj4tpspi"> <button id="back-to-top" class="focus-outline whitespace-nowrap py-1 hover:opacity-75 astro-vj4tpspi"> <svg xmlns="http://www.w3.org/2000/svg" class="rotate-90 astro-vj4tpspi"> <path d="M13.293 6.293 7.586 12l5.707 5.707 1.414-1.414L10.414 12l4.293-4.293z" class="astro-vj4tpspi"></path> </svg> <span class="astro-vj4tpspi">Back to Top</span> </button> ${renderComponent($$result2, "ShareLinks", $$ShareLinks, { "class": "astro-vj4tpspi" })} </div> <div class="reveal-on-scroll astro-vj4tpspi" data-reveal-target> ${renderComponent($$result2, "Comments", null, { "client:only": "react", "client:component-hydration": "only", "class": "astro-vj4tpspi", "client:component-path": "@components/Comments", "client:component-export": "default" })} </div> <hr class="my-6 border-dashed astro-vj4tpspi"> <!-- Previous/Next Post Buttons --> <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 astro-vj4tpspi"> ${prevPost && renderTemplate`<a${addAttribute(`/posts/${prevPost.slug}`, "href")} class="flex w-full gap-1 hover:opacity-75 astro-vj4tpspi"> <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon icon-tabler icons-tabler-outline icon-tabler-chevron-left flex-none astro-vj4tpspi"> ${renderComponent($$result2, "Fragment", Fragment, { "class": "astro-vj4tpspi" }, { "default": async ($$result3) => renderTemplate` <path stroke="none" d="M0 0h24v24H0z" fill="none" class="astro-vj4tpspi"></path> <path d="M15 6l-6 6l6 6" class="astro-vj4tpspi"></path> ` })} </svg> <div class="astro-vj4tpspi"> <span class="astro-vj4tpspi">Previous Post</span> <div class="text-sm text-skin-accent/85 astro-vj4tpspi">${prevPost.title}</div> </div> </a>`} ${nextPost && renderTemplate`<a${addAttribute(`/posts/${nextPost.slug}`, "href")} class="flex w-full justify-end gap-1 text-right hover:opacity-75 sm:col-start-2 astro-vj4tpspi"> <div class="astro-vj4tpspi"> <span class="astro-vj4tpspi">Next Post</span> <div class="text-sm text-skin-accent/85 astro-vj4tpspi">${nextPost.title}</div> </div> <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon icon-tabler icons-tabler-outline icon-tabler-chevron-right flex-none astro-vj4tpspi"> ${renderComponent($$result2, "Fragment", Fragment, { "class": "astro-vj4tpspi" }, { "default": async ($$result3) => renderTemplate` <path stroke="none" d="M0 0h24v24H0z" fill="none" class="astro-vj4tpspi"></path> <path d="M9 6l6 6l-6 6" class="astro-vj4tpspi"></path> ` })} </svg> </a>`} </div> </div> </div> </main> <div id="image-viewer" class="image-viewer astro-vj4tpspi" hidden role="dialog" aria-modal="true" aria-label="Image viewer"> <div class="image-viewer__content astro-vj4tpspi" role="document"> <figure class="image-viewer__figure astro-vj4tpspi"> <img id="image-viewer-img" class="image-viewer__image astro-vj4tpspi" alt="" loading="lazy"> </figure> </div> </div> ${renderComponent($$result2, "Footer", $$Footer, { "class": "astro-vj4tpspi" })} ` }));
}, "/Users/stevejobs/study/projects/vernonwu.com/src/layouts/PostDetails.astro", "self");

const $$Astro = createAstro("https://vernonwu.com/");
const prerender = true;
async function getStaticPaths() {
  const posts = await getCollection("blog", ({ data }) => !data.draft);
  const postResult = posts.map((post) => ({
    params: { slug: post.slug },
    props: { post }
  }));
  return postResult;
}
const $$Index = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Index;
  const { post } = Astro2.props;
  const posts = await getCollection("blog");
  const sortedPosts = getSortedPosts(posts);
  return renderTemplate`${renderComponent($$result, "PostDetails", $$PostDetails, { "post": post, "posts": sortedPosts })}`;
}, "/Users/stevejobs/study/projects/vernonwu.com/src/pages/posts/[slug]/index.astro", void 0);

const $$file = "/Users/stevejobs/study/projects/vernonwu.com/src/pages/posts/[slug]/index.astro";
const $$url = "/posts/[slug]";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Index,
  file: $$file,
  getStaticPaths,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
